# EV Charging Session Tracker

Full-stack EV charging session tracker. Monorepo scaffold — see `/client`
(React + Vite + TS + antd), `/server` (Express + TS), `/shared` (types used
by both).

Full setup instructions, architecture notes, and assumptions will land here
once the backend and frontend are built out.
